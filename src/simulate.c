#include "memory.h"
#include "read_elf.h"
#include "simulate.h"
#include <stdio.h>

struct Stat simulate(struct memory *mem, int start_addr, FILE *log_file, struct symbols* symbols){
    struct Stat dummy;
    return dummy;
}